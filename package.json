{
  "name": "faryma-stats",
  "version": "1.0.0",
  "private": true,
  "description": "Архив разносов farymastats.info. Сайт статический и сборки не требует — этот файл нужен только тестам.",
  "type": "module",
  "scripts": {
    "test": "node --test --test-timeout=180000 test/",
    "test:fast": "node --test test/parse.test.mjs test/archive.test.mjs"
  },
  "devDependencies": {
    "playwright": "1.56.1"
  }
}
